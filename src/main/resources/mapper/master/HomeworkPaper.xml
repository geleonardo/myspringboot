<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhihuishu.athomework.mapper.master.HomeworkPaperMapper">

    <!--获取试卷ID-->
    <select id="findPaperID" resultType="Long">
              SELECT PaperID FROM HomeworkPaper WHERE HomeworkID=#{homeworkId}
     </select>

</mapper>